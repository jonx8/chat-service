# Chat Service API
Микросервис для управления чатами и сообщениями с RESTful API.

## 📋 Описание
Сервис предоставляет HTTP API для:

- Создания и управления чатами
- Отправки сообщений в чаты
- Получения чатов с последними сообщениями
- Удаления чатов с каскадным удалением сообщений

## 🛠 Стек технологий
- Go 1.25.5
- PostgreSQL 15
- GORM - ORM для работы с базой данных
- net/http - стандартная HTTP библиотека Go
- goose - миграции базы данных
- golangci-lint - статический анализ кода
- testify - фреймворк для тестирования
- Docker & Docker Compose - контейнеризация

## 🚀 Быстрый старт
#### Предварительные требования
- Docker
- Docker Compose

#### Запуск
```bash
# Сборка и запуск приложения
docker compose up --build
```
#### Очистка
```bash
# Отключение и очистка
docker compose down -v
```

## Основные эндпоинты
1. Создание чата
```http
POST /chats
Content-Type: application/json

{
  "title": "Рабочий чат команды"
}
```
2. Получение чата с сообщениями
```http
GET /chats/{id}?limit=20
```
3. Отправка сообщения
```http
POST /chats/{id}/messages
Content-Type: application/json

{
  "text": "Привет всем!"
}
```
4. Удаление чата
```http
DELETE /chats/{id}
```
## 🧪 Тестирование
```bash
# Запуск всех тестов
go test ./...

# Или
make test
```


## Структура проекта
```text
.
├── cmd/
│   └── api/
│       └── main.go                 # Точка входа
├── internal/
│   ├── config/                     # Конфигурация
│   ├── database/                   # Подключение к БД
│   ├── models/                     # Модели данных
│   ├── repositories/               # Репозитории
│   ├── services/                   # Бизнес-логика
│   ├── handlers/                   # HTTP обработчики
│   └── dto/                        # Data Transfer Objects
├── migrations/                     # Миграции goose
├── docker-compose.yml
├── Dockerfile
├── Makefile
├── .golangci.yml                   # Конфигурация линтера
├── .env.example                    # Шаблон переменных окружения
└── README.md
```